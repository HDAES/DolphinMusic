import 'package:flutter/material.dart';

class DynamicIndex extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('DynamicIndex'),
    );
  }
}